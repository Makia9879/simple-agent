# B 线后台 UI 的包管理器配置。
# 开发 Compose 的 node:22-alpine 容器内首次 `pnpm install` 曾因 registry 下载
# 超时（20 KiB/s 级别）而失败退出；以下参数放宽重试与超时，避免网络抖动直接
# 终止容器（docs/deployment.md §2 的 Docker 开发闭环）。
fetch-timeout=300000
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
